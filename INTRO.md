# 项目介绍

本项目实现了基于 B 样条的 KAN 线性层，并在多个典型动力系统上进行数据驱动建模实验，包括二维三次阻尼振子、Lorenz 系统与糖酵解振荡器。主要代码位于 `BKAN.py`，其余 notebooks 负责数据生成、训练与可视化；`Cubic2D/` 目录中保存了不同平滑/多步方法（AM1、AM4、RTS、Savitzky-Golay）的对比结果与绘图脚本。

若需要对线性多步法基线进行对照，可参考 `MLNN-base/` 中的实现与说明。
